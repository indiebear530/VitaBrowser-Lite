name: Vita Build

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-vpk:
    runs-on: ubuntu-latest
    container:
      image: vitasdk/vitasdk:latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: apk add --no-cache make cmake ca-certificates openssl unzip curl

      - name: Build & install vita2dlib (using zip - more reliable)
        run: |
          curl -L -o /tmp/vita2d.zip https://github.com/vitasdk/vita2dlib/archive/refs/heads/master.zip
          unzip /tmp/vita2d.zip -d /tmp
          cd /tmp/vita2dlib-master
          make -j2
          make install

      - name: Configure project
        run: cmake -S . -B build -DCMAKE_TOOLCHAIN_FILE=${VITASDK}/share/vita.toolchain.cmake

      - name: Build project
        run: cmake --build build -- -j2

      - name: Upload VPK
        uses: actions/upload-artifact@v4
        with:
          name: VitaBrowserLite-VPK
          path: build/*.vpk
